#
# A list of additional NVRAM setting that can be inserted outside the 
# standard nvram setting
#
# NOTE: Not all boards need these parameters tunning.
#

#Setting wlx_awl_a2w_max_pktl_len as mentioned in CS00012074628
nvram set wl0_awl_a2w_max_pktl_len=1000
nvram set wl1_awl_a2w_max_pktl_len=1000

# Per interface pkt threshold: Sum total of all packets in the backup queues of
# flowrings belonging to the interface, not including packets already admitted
# to flowrings.
nvram set dhd_if_threshold=5000

