-- Returns a table that contains Section Name, Page Name and its Options
return {
  Modem = {
    modem = {
      configuration = {
        description = "To display the export/import configuration based on customer",
        value = true
      }
    }
  }
}
