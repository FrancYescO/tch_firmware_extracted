<document>
  <h2>NAT Configuration</h2>
  <p>The Network Address Translation (NAT) protocol enables multiple hosts in a private subnet to share a single public IP address to access the Internet.</p>
  <h4>NAT settings</h4>
  <p>Under NAT Configuration, you can configure the following settings:</p>
  <li1>
    <b>NAT</b>:<br></br>Allows you to enable or disable NAT.</li1>
</document>
