#!/bin/sh
[ -z "$1" ] && echo "Error: should be run by odhcpc" && exit 1

case "$1" in
        add|del)
                ip addr $1 ${dhcp_backup_ip:-192.168.1.2}/${dhcp_backup_mask:-24} dev ${interface}
        ;;
esac

exit 0
