set -e
[ -n "$1" ]

interface="$1"

. $IPKG_INSTROOT/lib/config/uci.sh

device=$(uci_get network $interface ifname)
stats=$(ubus call network.device status '{"name":"'"$device"'"}')

logger -t trafficmon.$interface $stats
